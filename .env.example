# =============================================================================
# TRADING MODELS API - ENVIRONMENT VARIABLES
# =============================================================================
# Copy this file to .env and fill in your actual values
# Never commit .env to git (it's in .gitignore)

# =============================================================================
# MONGODB (REQUIRED)
# =============================================================================
# Get from: https://cloud.mongodb.com
# 1. Create free cluster (M0)
# 2. Database Access -> Create user
# 3. Network Access -> Add IP (0.0.0.0/0 for Vercel)
# 4. Connect -> Connect your application -> Copy connection string

MONGODB_URI=mongodb+srv://username:password@cluster0.xxxxx.mongodb.net/?retryWrites=true&w=majority
MONGODB_DB=trading

# =============================================================================
# DATA PROVIDERS (OPTIONAL - Using Free Tiers)
# =============================================================================

# --- CRYPTO (Primary: Binance - NO KEY NEEDED) ---
# Binance has unlimited free tier for price data
# No API key required for public endpoints

# --- CRYPTO (Fallback: CoinGecko - NO KEY NEEDED) ---
# CoinGecko free tier: 10-50 calls/min
# No API key required for basic tier

# --- FOREX (Primary: Twelve Data - FREE 800/day) ---
# Get from: https://twelvedata.com (FREE tier)
# No API key needed for basic tier
# Upgrade if you need more than 800 calls/day

# --- FOREX (Fallback: EODHD - OPTIONAL) ---
# Get from: https://eodhd.com (Paid, ~$20/month)
# Only needed if Twelve Data quota exhausted
EODHD_KEY=your_eodhd_api_key_here

# --- FOREX (Fallback 2: Alpha Vantage - OPTIONAL) ---
# Get from: https://www.alphavantage.co (Free tier: 5 calls/min)
# Only needed if both above fail
ALPHA_VANTAGE_KEY=your_alpha_vantage_key_here

# =============================================================================
# VERCEL DEPLOYMENT (AUTO-SET)
# =============================================================================
# These are automatically set by Vercel
# VERCEL_URL=your-project.vercel.app
# VERCEL_ENV=production

# =============================================================================
# DEVELOPMENT
# =============================================================================
NODE_ENV=development
PORT=3000

# =============================================================================
# RATE LIMITING & CACHING
# =============================================================================
ENABLE_CACHE=true
CACHE_CRYPTO_HOURS=1
CACHE_FOREX_HOURS=4

# =============================================================================
# MONITORING (OPTIONAL)
# =============================================================================
# Add if you want external monitoring
# SENTRY_DSN=your_sentry_dsn
# LOGGING_LEVEL=info