name: Manual Predictions Trigger

on:
  workflow_dispatch:
    inputs:
      symbols_to_predict:
        description: 'Symbols (comma-separated, or leave empty for default)'
        required: false
        default: 'BTC/USDT,ETH/USDT,SOL/USDT,EURUSD,GBPUSD'

env:
  API_BASE_URL: ${{ secrets.VERCEL_DEPLOYMENT_URL || 'https://trading-models.vercel.app' }}

jobs:
  manual-predictions:
    name: Generate Predictions (Manual Trigger)
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
      
      - name: Display configuration
        run: |
          echo "🎯 API Base URL: ${{ env.API_BASE_URL }}"
          echo "📊 Symbols: ${{ github.event.inputs.symbols_to_predict || 'BTC/USDT,ETH/USDT,SOL/USDT,EURUSD,GBPUSD' }}"
      
      # ======================================================================
      # Predict: BTC/USDT
      # ======================================================================
      - name: Predict BTC/USDT
        continue-on-error: true
        run: |
          echo "🪙 Processing BTC/USDT..."
          
          # Fetch data
          echo "  📥 Fetching market data..."
          RESPONSE=$(curl -s "${{ env.API_BASE_URL }}/api/fetch-data?symbol=BTC%2FUSDT" --max-time 30 -w "\nHTTP_CODE:%{http_code}")
          
          HTTP_CODE=$(echo "$RESPONSE" | grep "HTTP_CODE:" | cut -d: -f2)
          BODY=$(echo "$RESPONSE" | sed '/HTTP_CODE:/d')
          
          if [ "$HTTP_CODE" != "200" ]; then
            echo "  ❌ Failed to fetch data (HTTP $HTTP_CODE)"
            exit 0
          fi
          
          if ! echo "$BODY" | jq empty 2>/dev/null; then
            echo "  ❌ Invalid JSON response"
            exit 0
          fi
          
          SUCCESS=$(echo "$BODY" | jq -r '.success // false')
          if [ "$SUCCESS" != "true" ]; then
            echo "  ❌ Data fetch failed"
            exit 0
          fi
          
          echo "  ✅ Data fetched successfully"
          
          # Extract data and run prediction
          DATA_PAYLOAD=$(echo "$BODY" | jq -c '.data')
          
          echo "  🤖 Running prediction..."
          PRED_RESPONSE=$(curl -s -X POST "${{ env.API_BASE_URL }}/api/predict-crypto" \
            -H "Content-Type: application/json" \
            --max-time 30 \
            -w "\nHTTP_CODE:%{http_code}" \
            -d "{\"symbol\":\"BTC/USDT\",\"data\":$DATA_PAYLOAD}")
          
          PRED_HTTP_CODE=$(echo "$PRED_RESPONSE" | grep "HTTP_CODE:" | cut -d: -f2)
          PRED_BODY=$(echo "$PRED_RESPONSE" | sed '/HTTP_CODE:/d')
          
          if [ "$PRED_HTTP_CODE" != "200" ]; then
            echo "  ❌ Prediction failed (HTTP $PRED_HTTP_CODE)"
            exit 0
          fi
          
          PRED_CLASS=$(echo "$PRED_BODY" | jq -r '.class // "UNKNOWN"')
          PRED_CONF=$(echo "$PRED_BODY" | jq -r '.confidence // 0')
          STORED=$(echo "$PRED_BODY" | jq -r '.stored // false')
          
          echo "  ✅ Prediction: $PRED_CLASS (Confidence: $PRED_CONF)"
          echo "  💾 Stored: $STORED"
      
      # ======================================================================
      # Predict: ETH/USDT
      # ======================================================================
      - name: Predict ETH/USDT
        continue-on-error: true
        run: |
          echo "🪙 Processing ETH/USDT..."
          
          RESPONSE=$(curl -s "${{ env.API_BASE_URL }}/api/fetch-data?symbol=ETH%2FUSDT" --max-time 30 -w "\nHTTP_CODE:%{http_code}")
          
          HTTP_CODE=$(echo "$RESPONSE" | grep "HTTP_CODE:" | cut -d: -f2)
          BODY=$(echo "$RESPONSE" | sed '/HTTP_CODE:/d')
          
          if [ "$HTTP_CODE" != "200" ]; then
            echo "  ❌ Failed to fetch data (HTTP $HTTP_CODE)"
            exit 0
          fi
          
          if ! echo "$BODY" | jq empty 2>/dev/null; then
            echo "  ❌ Invalid JSON response"
            exit 0
          fi
          
          SUCCESS=$(echo "$BODY" | jq -r '.success // false')
          if [ "$SUCCESS" != "true" ]; then
            echo "  ❌ Data fetch failed"
            exit 0
          fi
          
          echo "  ✅ Data fetched successfully"
          
          DATA_PAYLOAD=$(echo "$BODY" | jq -c '.data')
          
          echo "  🤖 Running prediction..."
          PRED_RESPONSE=$(curl -s -X POST "${{ env.API_BASE_URL }}/api/predict-crypto" \
            -H "Content-Type: application/json" \
            --max-time 30 \
            -w "\nHTTP_CODE:%{http_code}" \
            -d "{\"symbol\":\"ETH/USDT\",\"data\":$DATA_PAYLOAD}")
          
          PRED_HTTP_CODE=$(echo "$PRED_RESPONSE" | grep "HTTP_CODE:" | cut -d: -f2)
          PRED_BODY=$(echo "$PRED_RESPONSE" | sed '/HTTP_CODE:/d')
          
          if [ "$PRED_HTTP_CODE" != "200" ]; then
            echo "  ❌ Prediction failed (HTTP $PRED_HTTP_CODE)"
            exit 0
          fi
          
          PRED_CLASS=$(echo "$PRED_BODY" | jq -r '.class // "UNKNOWN"')
          PRED_CONF=$(echo "$PRED_BODY" | jq -r '.confidence // 0')
          STORED=$(echo "$PRED_BODY" | jq -r '.stored // false')
          
          echo "  ✅ Prediction: $PRED_CLASS (Confidence: $PRED_CONF)"
          echo "  💾 Stored: $STORED"
      
      # ======================================================================
      # Predict: SOL/USDT
      # ======================================================================
      - name: Predict SOL/USDT
        continue-on-error: true
        run: |
          echo "🪙 Processing SOL/USDT..."
          
          RESPONSE=$(curl -s "${{ env.API_BASE_URL }}/api/fetch-data?symbol=SOL%2FUSDT" --max-time 30 -w "\nHTTP_CODE:%{http_code}")
          
          HTTP_CODE=$(echo "$RESPONSE" | grep "HTTP_CODE:" | cut -d: -f2)
          BODY=$(echo "$RESPONSE" | sed '/HTTP_CODE:/d')
          
          if [ "$HTTP_CODE" != "200" ]; then
            echo "  ❌ Failed to fetch data (HTTP $HTTP_CODE)"
            exit 0
          fi
          
          if ! echo "$BODY" | jq empty 2>/dev/null; then
            echo "  ❌ Invalid JSON response"
            exit 0
          fi
          
          SUCCESS=$(echo "$BODY" | jq -r '.success // false')
          if [ "$SUCCESS" != "true" ]; then
            echo "  ❌ Data fetch failed"
            exit 0
          fi
          
          echo "  ✅ Data fetched successfully"
          
          DATA_PAYLOAD=$(echo "$BODY" | jq -c '.data')
          
          echo "  🤖 Running prediction..."
          PRED_RESPONSE=$(curl -s -X POST "${{ env.API_BASE_URL }}/api/predict-crypto" \
            -H "Content-Type: application/json" \
            --max-time 30 \
            -w "\nHTTP_CODE:%{http_code}" \
            -d "{\"symbol\":\"SOL/USDT\",\"data\":$DATA_PAYLOAD}")
          
          PRED_HTTP_CODE=$(echo "$PRED_RESPONSE" | grep "HTTP_CODE:" | cut -d: -f2)
          PRED_BODY=$(echo "$PRED_RESPONSE" | sed '/HTTP_CODE:/d')
          
          if [ "$PRED_HTTP_CODE" != "200" ]; then
            echo "  ❌ Prediction failed (HTTP $PRED_HTTP_CODE)"
            exit 0
          fi
          
          PRED_CLASS=$(echo "$PRED_BODY" | jq -r '.class // "UNKNOWN"')
          PRED_CONF=$(echo "$PRED_BODY" | jq -r '.confidence // 0')
          STORED=$(echo "$PRED_BODY" | jq -r '.stored // false')
          
          echo "  ✅ Prediction: $PRED_CLASS (Confidence: $PRED_CONF)"
          echo "  💾 Stored: $STORED"
      
      # ======================================================================
      # Predict: EURUSD
      # ======================================================================
      - name: Predict EURUSD
        continue-on-error: true
        run: |
          echo "💱 Processing EURUSD..."
          
          RESPONSE=$(curl -s "${{ env.API_BASE_URL }}/api/fetch-data?symbol=EURUSD" --max-time 45 -w "\nHTTP_CODE:%{http_code}")
          
          HTTP_CODE=$(echo "$RESPONSE" | grep "HTTP_CODE:" | cut -d: -f2)
          BODY=$(echo "$RESPONSE" | sed '/HTTP_CODE:/d')
          
          if [ "$HTTP_CODE" != "200" ]; then
            echo "  ❌ Failed to fetch data (HTTP $HTTP_CODE)"
            exit 0
          fi
          
          if ! echo "$BODY" | jq empty 2>/dev/null; then
            echo "  ❌ Invalid JSON response"
            exit 0
          fi
          
          SUCCESS=$(echo "$BODY" | jq -r '.success // false')
          if [ "$SUCCESS" != "true" ]; then
            echo "  ❌ Data fetch failed"
            exit 0
          fi
          
          echo "  ✅ Data fetched successfully"
          
          DATA_PAYLOAD=$(echo "$BODY" | jq -c '.data')
          
          echo "  🤖 Running prediction..."
          PRED_RESPONSE=$(curl -s -X POST "${{ env.API_BASE_URL }}/api/predict-forex" \
            -H "Content-Type: application/json" \
            --max-time 30 \
            -w "\nHTTP_CODE:%{http_code}" \
            -d "{\"pair\":\"EURUSD\",\"data\":$DATA_PAYLOAD}")
          
          PRED_HTTP_CODE=$(echo "$PRED_RESPONSE" | grep "HTTP_CODE:" | cut -d: -f2)
          PRED_BODY=$(echo "$PRED_RESPONSE" | sed '/HTTP_CODE:/d')
          
          if [ "$PRED_HTTP_CODE" != "200" ]; then
            echo "  ❌ Prediction failed (HTTP $PRED_HTTP_CODE)"
            exit 0
          fi
          
          PRED_CLASS=$(echo "$PRED_BODY" | jq -r '.class // "UNKNOWN"')
          PRED_CONF=$(echo "$PRED_BODY" | jq -r '.confidence // 0')
          STORED=$(echo "$PRED_BODY" | jq -r '.stored // false')
          
          echo "  ✅ Prediction: $PRED_CLASS (Confidence: $PRED_CONF)"
          echo "  💾 Stored: $STORED"
      
      # ======================================================================
      # Predict: GBPUSD
      # ======================================================================
      - name: Predict GBPUSD
        continue-on-error: true
        run: |
          echo "💱 Processing GBPUSD..."
          
          RESPONSE=$(curl -s "${{ env.API_BASE_URL }}/api/fetch-data?symbol=GBPUSD" --max-time 45 -w "\nHTTP_CODE:%{http_code}")
          
          HTTP_CODE=$(echo "$RESPONSE" | grep "HTTP_CODE:" | cut -d: -f2)
          BODY=$(echo "$RESPONSE" | sed '/HTTP_CODE:/d')
          
          if [ "$HTTP_CODE" != "200" ]; then
            echo "  ❌ Failed to fetch data (HTTP $HTTP_CODE)"
            exit 0
          fi
          
          if ! echo "$BODY" | jq empty 2>/dev/null; then
            echo "  ❌ Invalid JSON response"
            exit 0
          fi
          
          SUCCESS=$(echo "$BODY" | jq -r '.success // false')
          if [ "$SUCCESS" != "true" ]; then
            echo "  ❌ Data fetch failed"
            exit 0
          fi
          
          echo "  ✅ Data fetched successfully"
          
          DATA_PAYLOAD=$(echo "$BODY" | jq -c '.data')
          
          echo "  🤖 Running prediction..."
          PRED_RESPONSE=$(curl -s -X POST "${{ env.API_BASE_URL }}/api/predict-forex" \
            -H "Content-Type: application/json" \
            --max-time 30 \
            -w "\nHTTP_CODE:%{http_code}" \
            -d "{\"pair\":\"GBPUSD\",\"data\":$DATA_PAYLOAD}")
          
          PRED_HTTP_CODE=$(echo "$PRED_RESPONSE" | grep "HTTP_CODE:" | cut -d: -f2)
          PRED_BODY=$(echo "$PRED_RESPONSE" | sed '/HTTP_CODE:/d')
          
          if [ "$PRED_HTTP_CODE" != "200" ]; then
            echo "  ❌ Prediction failed (HTTP $PRED_HTTP_CODE)"
            exit 0
          fi
          
          PRED_CLASS=$(echo "$PRED_BODY" | jq -r '.class // "UNKNOWN"')
          PRED_CONF=$(echo "$PRED_BODY" | jq -r '.confidence // 0')
          STORED=$(echo "$PRED_BODY" | jq -r '.stored // false')
          
          echo "  ✅ Prediction: $PRED_CLASS (Confidence: $PRED_CONF)"
          echo "  💾 Stored: $STORED"
      
      # ======================================================================
      # Verification
      # ======================================================================
      - name: Verify stored predictions
        continue-on-error: true
        run: |
          echo "🔍 Checking database..."
          
          # FIX: Simple health check instead of full fetch
          RESPONSE=$(curl -s "${{ env.API_BASE_URL }}/api/store-prediction?health=true" \
            --max-time 5 \
            -w "\nHTTP_CODE:%{http_code}")
          
          HTTP_CODE=$(echo "$RESPONSE" | grep "HTTP_CODE:" | cut -d: -f2)
          BODY=$(echo "$RESPONSE" | sed '/HTTP_CODE:/d')
          
          if [ "$HTTP_CODE" = "200" ]; then
            echo "✅ Storage service healthy"
            
            # Extract count if available
            TOTAL=$(echo "$BODY" | jq -r '.total_predictions // 0' 2>/dev/null || echo "0")
            echo "Total predictions in database: $TOTAL"
          else
            echo "⚠️  Storage service returned HTTP $HTTP_CODE"
            echo "Response: $BODY"
          fi
      
      - name: Summary
        if: always()
        run: |
          echo ""
          echo "========================================================================"
          echo "✅ MANUAL PREDICTIONS COMPLETE"
          echo "========================================================================"
          echo ""
          echo "🌐 Dashboard: ${{ env.API_BASE_URL }}"
          echo ""
          echo "Refresh your dashboard to see the new predictions!"
          echo ""